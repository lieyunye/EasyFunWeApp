<comment_cell comment="{{comment}}">
</comment_cell>
<block wx:for="{{comment_list}}" wx:key="*this" wx:if="{{comment_list.length > 0}}">
  <comment_cell comment="{{item}}">
  </comment_cell>
</block>